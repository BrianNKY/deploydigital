<%= form_with(model: cgu, local: true) do |form| %>
  <% if cgu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cgu.errors.count, "error") %> prohibited this cgu from being saved:</h2>

      <ul>
      <% cgu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
